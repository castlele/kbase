struct Steve: Player {
    let type = ElementType.player
}

struct GrassMinecraftBlock: Block {
    let blockType = "grass"
    let type = ElementType.block
}

struct LeavesMinecraftBlock: Block {
    let blockType = "leaves"
    let type = ElementType.block
}

struct AirMinecraftBlock: Block {
    let blockType = "air"
    let type = ElementType.block
}

